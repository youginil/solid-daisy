import { Component } from "solid-js";

const Home: Component = () => {
    return (
        <div>
            <button class="btn btn-primary">Hello</button>
        </div>
    );
};

export default Home;
